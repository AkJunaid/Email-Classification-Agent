services:
  email-agent:
    build: ./app
    container_name: email_agent_dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./app:/app
      - ./dataset:/app/dataset:ro
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped